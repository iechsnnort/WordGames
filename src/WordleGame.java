public class WordleGame {
    private String currentWord;
    private static double highScore;

    public void WordleGame(String word) {
        // Initialize the Wordle game
    }

    public void run() {
        do {
            // This is where the game loop and game logic happens
        } while (true);
    }
}
